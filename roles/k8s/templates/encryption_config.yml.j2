kind: EncryptionConfig
apiVersion: v1
resources:
  - resources:
      - secrets
    providers:
      - aescbc:
          keys:
            - name: key1
              secret: "{{ pillar.k8s_comp.api_server.encryption_token }}"
      - identity: {}
