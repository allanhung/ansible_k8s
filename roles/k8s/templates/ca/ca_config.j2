{
  "signing": {
    "default": {
      "expiry": "{{ pillar.ca_common.expiry }}"
    },
    "profiles": {
      "kubernetes": {
        "usages": [
          "signing",
          "key encipherment",
          "server auth",
          "client auth"
        ],
        "expiry": "{{ pillar.ca_common.expiry }}"
      }
    }
  }
}
